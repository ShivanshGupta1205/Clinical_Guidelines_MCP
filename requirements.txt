# Pinned per docs/PROJECT_BRIEF.md "Tech stack" section.

# MCP server
mcp

# Postgres + pgvector
psycopg[binary]
pgvector

# Embeddings (local, no paid API — deliberate choice, see docs/CLAUDE.md)
sentence-transformers

# openFDA ingestion (HTTP + JSON; no scraping, no PDF parsing)
httpx

# Testing
pytest
